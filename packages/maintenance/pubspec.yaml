name: maintenance
description: The client of AskA Maintenance.
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"

resolution: workspace

dependencies:
  cookie_jar: ^4.0.0
  dio: ^5.0.0
  html: ^0.15.0
  http_parser: ^4.0.0
  intl: any
  mobx: ^2.0.0

dev_dependencies:
  build_runner: ^2.0.0
  lints: ^6.0.0
  mobx_codegen: ^2.0.0
