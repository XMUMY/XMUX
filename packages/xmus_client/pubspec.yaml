name: xmus_client
description: Dart client of XMUS services.
publish_to: none

environment:
  sdk: ">=3.11.0 <4.0.0"

resolution: workspace

dependencies:
  grpc: ^5.0.0
  protobuf: ^6.0.0

dev_dependencies:
  lints: ^6.0.0
