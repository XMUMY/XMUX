/*
// Fallback version.

const fallbackBuildName = String.fromEnvironment(
  'FLUTTER_BUILD_NAME',
  defaultValue: '3.3.0-SNAPSHOT',
);
const fallbackBuildNumber = int.fromEnvironment(
  'FLUTTER_BUILD_NUMBER',
  defaultValue: 1,
);

// 3rd-party app key.

const wolframAppId = '';
const sentryDsn = '';

// Backend connection.

const apiAddress = 'https://example.com';
const attendanceAddress = 'http://example.com';
const moodleAddress = 'https://l.xmu.edu.my';

// Sites.

const websiteAddress = 'https://xmux.xdea.io';
const docsAddress = 'https://docs.xmux.xdea.io';
*/